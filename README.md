# Kalman-Filter-using-c-
This C++ Kalman filter estimates 1D projectile motion states (position, velocity, acceleration) from noisy position sensor inputs using the Eigen library. It executes recursive predict and update cycles via linear system matrices (F, H, Q, R, P), dynamically computing the Kalman gain to fuse physical motion models with real-time measurements.
